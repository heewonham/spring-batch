logging:
  level:
    root: info

spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql_database/payment_db?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: test
    password: 1234
  batch:
    jdbc:
      initialize-schema: always
  sql:
    init:
      mode: always
      encoding: UTF-8
#    job:
#      names: ${job.name:NONE}